#include <stdio.h>

/**
 *main - This receives no argument
 *
 *Return: Alwaysw 0 (Success)
 */

int main(void)
{
	char alpha1, alpha2;

	for (al1 = 'a'; al1 <= 'z'; al1++)
	{
		putchar(al1);
	}

	for (al2 = 'A'; al2 <= 'Z'; 2++)
	{
		putchar(al2);
	}
	putchar('\n');
	return (0);
}
